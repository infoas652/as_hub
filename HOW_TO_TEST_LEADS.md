# 🧪 دليل اختبار صفحة الرسائل (Leads)

## ✅ التحقق من البيانات

### 1. البيانات في قاعدة البيانات ✅
```bash
php test-leads-api.php
```
**النتيجة:** 6 رسائل موجودة ✅

---

## 🚀 خطوات الاختبار

### الخطوة 1: تشغيل Backend
```bash
cd backend
php artisan serve --port=8000
```
✅ يجب أن يعمل على: http://localhost:8000

### الخطوة 2: تشغيل Admin Panel
```bash
cd admin-panel
ng serve --port 4202
```
✅ يجب أن يعمل على: http://localhost:4202

### الخطوة 3: تسجيل الدخول
1. افتح: http://localhost:4202
2. سجل دخول:
   - Email: `admin@ashub.com`
   - Password: `Admin@123456`

### الخطوة 4: اختبار صفحة Leads
1. **من القائمة الجانبية:** اضغط على "الرسائل" (Leads) 📧
2. **يجب أن تشاهد:**
   - جدول يحتوي على 6 رسائل
   - أعمدة: ID, Name, Email, Phone, Company, Plan, Status, Date, Actions

### الخطوة 5: اختبار الوظائف

#### أ) الفلاتر
- **Status Filter:** جرب تغيير الحالة (All, New, Contacted, etc.)
- **Processed Filter:** جرب (All, Processed Only, Unprocessed Only)
- **Search:** ابحث عن اسم (مثل: "John")

#### ب) عرض التفاصيل
- اضغط على أيقونة العين 👁️ بجانب أي رسالة
- يجب أن يفتح Modal يعرض جميع التفاصيل

#### ج) تعليم كمعالج
- اضغط على أيقونة ✓ بجانب رسالة غير معالجة
- يجب أن تتحول إلى "Processed"

#### د) حذف رسالة
- اضغط على أيقونة 🗑️
- أكد الحذف
- يجب أن تختفي الرسالة من الجدول

#### هـ) تصدير CSV
- اضغط على زر "تصدير CSV" في الأعلى
- يجب أن يتم تحميل ملف CSV يحتوي على جميع الرسائل

#### و) تبديل اللغة
- اضغط على زر اللغة في الأعلى (🇬🇧 EN / 🇸🇦 AR)
- يجب أن تتغير جميع النصوص
- يجب أن يتغير الاتجاه (LTR ↔ RTL)

---

## 🔍 إذا لم تظهر الرسائل

### تحقق من Console في المتصفح
1. افتح Developer Tools (F12)
2. اذهب إلى Console
3. ابحث عن أخطاء (Errors باللون الأحمر)

### الأخطاء الشائعة وحلولها

#### 1. خطأ 401 Unauthorized
**المشكلة:** Token منتهي أو غير صحيح
**الحل:** 
- سجل خروج
- سجل دخول مرة أخرى

#### 2. خطأ 404 Not Found
**المشكلة:** API endpoint غير موجود
**الحل:**
```bash
# تحقق من routes
php backend/artisan route:list | grep leads
```

#### 3. خطأ CORS
**المشكلة:** CORS غير مفعّل
**الحل:**
```bash
# تحقق من config/cors.php
# تأكد أن localhost:4202 موجود في allowed_origins
```

#### 4. الصفحة فارغة (No errors)
**المشكلة:** Component لم يتم تحميله
**الحل:**
```bash
# أعد بناء المشروع
cd admin-panel
ng build
ng serve --port 4202
```

---

## 🧪 اختبار API مباشرة

### باستخدام curl (في Terminal)

#### 1. تسجيل الدخول والحصول على Token
```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"admin@ashub.com\",\"password\":\"Admin@123456\"}"
```

**احفظ الـ access_token من الرد**

#### 2. جلب الرسائل
```bash
curl -X GET "http://localhost:8000/api/admin/leads?per_page=10" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -H "Accept: application/json"
```

**يجب أن ترى:** JSON يحتوي على 6 رسائل

---

## 📊 النتيجة المتوقعة

### في صفحة Leads يجب أن تشاهد:

```
الرسائل
إدارة استفسارات العملاء والرسائل

[🔄 تحديث] [📥 تصدير CSV]

[Status: All ▼] [Processed: All ▼] [🔍 Search...]

┌────┬──────────────┬─────────────────────────┬─────────────────┬──────────────────┬──────────────┬──────────┬──────────────┬──────────┐
│ ID │ Name         │ Email                   │ Phone           │ Company          │ Plan         │ Status   │ Date         │ Actions  │
├────┼──────────────┼─────────────────────────┼─────────────────┼──────────────────┼──────────────┼──────────┼──────────────┼──────────┤
│ 6  │ Ahmed Ali    │ ahmed@company.sa        │ +966 50 123...  │ Saudi Tech       │ enterprise   │ New      │ Today        │ 👁️ ✓ 🗑️ │
│ 5  │ Emily Davis  │ emily.davis@business... │ +1 (555) 456... │ Business Solu... │ professional │ Qualified│ Today        │ 👁️ 🗑️   │
│ 4  │ Michael...   │ mbrown@startup.io       │ +1 (555) 345... │ StartupXYZ       │ basic        │ New      │ Today        │ 👁️ ✓ 🗑️ │
│ 3  │ Sarah...     │ sarah.j@company.com     │ +1 (555) 234... │ Digital Mark...  │ enterprise   │ Contacted│ Today        │ 👁️ ✓ 🗑️ │
│ 2  │ John Smith   │ john.smith@example.com  │ +1 (555) 123... │ Tech Solutions   │ professional │ New      │ Today        │ 👁️ ✓ 🗑️ │
│ 1  │ شسي          │ abood.ghalayini@...     │ 0595466383      │ شسييشس           │ enterprise   │ New      │ Today        │ 👁️ ✓ 🗑️ │
└────┴──────────────┴─────────────────────────┴─────────────────┴──────────────────┴──────────────┴──────────┴──────────────┴──────────┘
```

---

## ✅ Checklist

- [ ] Backend يعمل على port 8000
- [ ] Admin Panel يعمل على port 4202
- [ ] تسجيل الدخول ناجح
- [ ] صفحة Leads تفتح من القائمة
- [ ] الجدول يعرض 6 رسائل
- [ ] الفلاتر تعمل
- [ ] Modal التفاصيل يفتح
- [ ] تعليم كمعالج يعمل
- [ ] الحذف يعمل
- [ ] تصدير CSV يعمل
- [ ] تبديل اللغة يعمل

---

## 📸 لقطات الشاشة المتوقعة

### 1. صفحة Leads (فارغة)
إذا لم تظهر الرسائل، ستشاهد:
```
📧
No Leads Found
No customer inquiries yet. Leads will appear here when customers contact you.
```

### 2. صفحة Leads (مع بيانات)
جدول احترافي مع 6 صفوف

### 3. Modal التفاصيل
نافذة منبثقة تعرض:
- Name
- Email
- Phone
- Company
- Plan
- Status
- Message (نص كامل)
- Date

---

## 🆘 الدعم

إذا واجهت أي مشكلة:

1. **تحقق من Console** (F12 → Console)
2. **تحقق من Network** (F12 → Network → XHR)
3. **تحقق من Backend Logs** (`backend/storage/logs/laravel.log`)
4. **أعد تشغيل كل شيء:**
   ```bash
   # أوقف كل شيء (Ctrl+C)
   # Backend
   cd backend && php artisan serve --port=8000
   # Admin Panel (terminal جديد)
   cd admin-panel && ng serve --port 4202
   ```

---

**تم إنشاء هذا الدليل بواسطة BLACKBOX AI** ✨
